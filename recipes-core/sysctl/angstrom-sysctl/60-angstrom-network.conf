# PMTU blackhole recovery. Only tcp_mtu_probing is a real change; the other
# two are kernel defaults, pinned so a BSP/kernel bump cannot move them.
net.ipv4.ip_no_pmtu_disc = 0
net.ipv4.tcp_mtu_probing = 1
net.ipv4.tcp_base_mss = 1024
