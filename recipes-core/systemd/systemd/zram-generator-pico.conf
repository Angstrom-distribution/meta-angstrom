# zram config for pico: lzo-rle compression at 100% of physical RAM.
# lzo-rle is the least CPU-intensive choice at this size on a 200 MHz core.
[zram0]
compression-algorithm = lzo-rle
zram-size = ram
